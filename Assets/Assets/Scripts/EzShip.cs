using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EzShip : DamgeScript
{
    // Start is called before the first frame update
    void Start()
    {
        life = 50f;
        damage = 50f;
        //gameObject.tag = "Enemy";
    }
}

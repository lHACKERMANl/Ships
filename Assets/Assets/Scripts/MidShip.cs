using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MidShip : DamgeScript
{
    // Start is called before the first frame update
    void Start()
    {
        life = 100f;
        damage = 100f;
        //gameObject.tag = "Enemy";
    }
}
